kind = "web"
previewPath = "/"
title = "Origins: The Unbound"
version = "1.0.0"
id = "artifacts/game"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 8000

[services.development]
run = "cd /home/runner/workspace && python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload"
