You are extending an EXISTING frontend + backend system.

DO NOT redesign architecture.
DO NOT remove existing combat or navigation.

You must ONLY extend how nodes behave when entered.

---

GOAL:

Replace instant node actions with an interaction system.

Players should enter nodes and choose actions instead of immediately starting combat.

---

1. NODE ENTRY FLOW

Current:
Click node → immediate combat

New:
Click node → open node interaction screen

---

2. NODE STATE

Each node has:

state:

* "unvisited"
* "visited"
* "cleared"

Update state accordingly after interaction.

---

3. INTERACTION OPTIONS

Each node type must present options.

---

COMBAT NODE:

Options:

* "Engage Enemy" → starts combat
* "Study Environment" → small bonus (e.g. +1 insight or reveal modifier)
* "Leave" → return to map

---

TRIAL NODE:

Options:

* "Begin Trial" → starts trial combat
* "Observe" → reveal god/modifiers
* "Leave"

---

ANOMALY NODE:

Options:

* "Investigate" → start challenge
* "Stabilize" → harder version (optional)
* "Leave"

---

TAVERN NODE:

Options:

* "Rest" → heal player
* "Change Equipment"
* "Leave"

---

LAZI NODE:

Options:

* "Listen"
* "Ignore"

Behavior:

Lazi should:

* comment on player mistakes (use codex data)
* suggest weak topics
* hint at nearby danger

DO NOT make him a menu system.
He should feel like a character.

---

4. FRONTEND

* Add a Node Interaction Screen
* Reuse existing screen system (NO routing changes)
* Show:

  * node name
  * biome
  * difficulty
  * options

---

5. BACKEND

* Add minimal support for:

  * node state updates
  * passing modifiers/god into combat

DO NOT rewrite API.

---

6. GAME FLOW

New loop:

Move → Enter Node → Choose Action → Outcome → Return to map

---

7. IMPORTANT RULES

* Do NOT break combat system
* Do NOT change how questions work
* Do NOT introduce new large systems
* Keep options simple (max 3 per node)

---

OUTPUT REQUIRED:

* Node interaction handler logic
* Example UI structure (React)
* Minimal backend updates

Focus on integration, not redesign.
